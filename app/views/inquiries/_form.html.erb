<%= simple_form_for(@inquiry, html: { class: 'form-horizontal' }) do |f| %>
<label class="string required control-label" for="inquiry_role">Role</label>
<div class="form-group string required inquiry_role">
    <%= f.collection_radio_buttons :role, [["Host","Host"], ["Waiter","Waiter"]], :first, :last %>
    </div>
    <%= f.input :city %>
    <%= f.input :name %>
    <%= f.input :email, as: :email %>
    <%= f.input :phone, as: :tel %>
    <%= f.input :date, as: :date, :order => [:month, :day, :year] %>
    <label class="string required control-label" for="inquiry_license">License</label>
    <%= f.collection_radio_buttons :license, [[true, "Yes"], [false,"No"]], :first, :last %>
    <div><%= f.button :submit %></div> 
<% end %>