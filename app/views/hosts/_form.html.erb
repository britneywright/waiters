<%= form_for(@host, remote: true, html: {role: :form, 'data-model' => 'host'}) do |f| %>
  <% if @host.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@host.errors.count, "error") %> prohibited this host from being saved:</h2>

      <ul>
      <% @host.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <%= f.fields_for :events do |events_form| %>
  <div class="form-group">
    <%= events_form.label :event_date, class: 'control-label' %><br>
    <%= events_form.date_select :event_date, class: 'form-control' %>
    <span class="help-block"></span>
  </div>
    <div class="form-group">
    <%= events_form.label :city, class: 'control-label' %><br>
    <%= events_form.text_field :city, class: 'form-control' %>
    <span class="help-block"></span>
  </div>
  <% end %>
  <div class="form-group">
    <%= f.label :name, class: 'control-label' %><br>
    <%= f.text_field :name, class: 'form-control' %>
    <span class="help-block"></span>    
  </div>
  <div class="form-group">
    <%= f.label :email, class: 'control-label' %><br>
    <%= f.text_field :email, class: 'form-control' %>
    <span class="help-block"></span>    
  </div>
  <div class="form-group">
    <%= f.label :phone, class: 'control-label' %><br>
    <%= f.text_field :phone, class: 'form-control' %>
    <span class="help-block"></span>    
  </div>
  <div class="actions">
    <%= f.submit 'Submit', class: 'btn btn-danger' %>
  </div>
<% end %>